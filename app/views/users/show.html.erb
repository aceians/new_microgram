<h1>You are successfully Logged in</h1></center>

<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
      <div>
        <button href="/signup">Upload Profile</button>
      </div>
    </section>
  </aside>
</div>

<center> 
<table style="width:100%">
  <tr>
    <th>Name</th>
    <th>Email</th> 
    <th>Department</th>
  </tr>
  <tr>
    <td><%= @user.name %></td>
    <td><%= @user.email %></td> 
    <td><%= @user.dept %></td>
  </tr>
</center>